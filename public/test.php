if($_SERVER['REQUEST_METHOD']==="GET")
    {
        $url=$_SERVER['REQUEST_URI'];
        if(strpos("?",$url))
        {
            $url=explode("?",$url);
            $endpoint=$url[0];
            if key_exists($url,)
            {

            }
        }
        else
        {
          if  
        }

    }